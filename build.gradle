plugins {
    id 'java-library'
}

allprojects {
    apply plugin: 'java-library'
    group 'dev.ayu'
    sourceCompatibility = JavaVersion.VERSION_17
}

defaultTasks 'build'

repositories {
    mavenCentral()
}

dependencies {
    // No dependencies :)
}
